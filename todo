look at state visitation, value function
standardize observation, moving estimate
don't shift the reward, only rescale it, otherwise you loose meaning

find way to diversity the z networks. Right now the trajectories are collected using the average z value for each, so the only diversity comes from the initialization. 

how are we handling exploration? 


